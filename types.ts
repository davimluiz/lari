export interface Memory {
  id: string;
  url: string;
  caption: string;
  poem: string;
  rotation: number;
  zIndex: number;
  xOffset: number; // Percentage for desktop scatter
  yOffset: number; // Percentage for desktop scatter
}
